{
  "name": "onec-cluster-manager-vscode",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "backend",
    "extension"
  ],
  "scripts": {
    "build": "npm run build --workspace @onec-cluster-manager/backend && npm run build --workspace onec-cluster-manager",
    "test": "npm run test --workspace @onec-cluster-manager/backend && npm run test --workspace onec-cluster-manager",
    "check": "npm run build && npm test"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=20"
  }
}

