# Полное покрытие `rac` — план выполнения

## 1. Исследование и capabilities

- [x] В `backend/src/rac/client.ts` добавить безопасный запуск справки и получение версии без endpoint RAS.
- [x] В `backend/src/rac/capabilities.ts` разобрать режимы `rac`; покрыть фикстурами и реальным прогоном 8.3.24/8.3.27/8.5.
- [x] В `backend/src/server.ts` добавить `GET /api/connections/:id/capabilities`; проверить, что недоступные режимы не исполняются.

## 2. Backend и модели

- [x] Расширить ресурсы `rules`, `profiles`, `counters`, `limits`, `service-settings`, `binary-data-storages` с корректными областями server/infobase и агрегацией.
- [x] Добавить универсальное чтение `info` для кластера, рабочего сервера, процесса, правила, профиля, счётчика и ограничения.
- [x] Добавить валидируемое изменение свойств рабочего сервера; оставить опасные операции за явным подтверждением клиента.
- [x] Дополнить тесты маршрутизации, фильтров, авторизации, capabilities и безопасной передачи аргументов без shell.

## 3. Расширение VS Code

- [x] Обновить `extension/src/model.ts`, `api.ts` и дерево с учётом capabilities.
- [x] Сделать базу раскрываемой и добавить дочерние `Сеансы`, `Соединения`, `Блокировки` с фильтром `infobase`.
- [x] Открывать все коллекции центральными таблицами с поиском, обновлением и CSV-экспортом.
- [x] Добавить карточки процесса и рабочего сервера; свойства сервера сохранять через backend.
- [x] Сохранить контекстные безопасные действия и проверку подтверждения для terminate/disconnect/turn-off/unregister.

## 4. MCP

- [x] Добавить discovery/capabilities и read-tools для всех поддерживаемых коллекций и карточек.
- [x] Добавить фильтрованные tools базы для сеансов, соединений и блокировок.
- [x] Добавить безопасные mutation-tools с Zod-валидацией и литералами подтверждения.
- [x] Проверить parity REST ↔ MCP протокольными тестами.

## 5. Проверка и поставка

- [x] Выполнить `npm run check`.
- [x] Выполнить read-only integration matrix на локальных 8.3.24, 8.3.27 и 8.5.
- [x] Выполнить безопасный E2E временной базы в изолированной СУБД.
- [x] Выполнить визуальный тест Extension Host и сохранить снимки центральных таблиц/карточек.
- [x] Обновить README с матрицей функций и ограничениями.
- [ ] Закоммитить, отправить ветку `codex/complete-rac-management` и открыть отдельный PR.
